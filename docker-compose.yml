version: '3'
docker pull ghcr.io/unity-technologies/kube_remediator:0.0.0
services:

  jekyll:
    image: jekyll/jekyll:3
    volumes:
      - ./:/srv/jekyll
    ports:
      - 4000:4000
    command: ["jekyll", "serve"]

#This service is just for building the site
  build:
    image: jekyll/jekyll:3
    volumes:
      - ./:/srv/jekyll
    command: ""
